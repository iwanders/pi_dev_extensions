# Pi.dev extensions

- *notify* Sends a `notify-send` notification after long running tasks, also exposes a tool to send notifications directly.
- *prevent_slash_non_command* Prevents sending messages that start with a slash but aren't commands to the LLM.
